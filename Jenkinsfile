pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                script {
                    sh 'npm --version'
                    sh 'npm i'
                }
            }
        }
    }
}
