pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'echo --- executing pipeline ---'
                sh 'echo ${foo}'
            }
        }
    }
}
