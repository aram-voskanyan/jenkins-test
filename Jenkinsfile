pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                script {
                    sh 'ls -alhR'
                    sh 'npm i'
                }
            }
        }
    }
}
